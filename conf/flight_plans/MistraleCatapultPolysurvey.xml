<!DOCTYPE flight_plan SYSTEM "flight_plan.dtd">

<flight_plan alt="235" ground_alt="185" lat0="43.46223" lon0="1.27289" max_dist_from_home="1500" name="Basic" qfu="300" security_height="25">
  <header>
#include "subsystems/datalink/datalink.h"
</header>
  <waypoints>
    <waypoint name="HOME" x="0" y="0"/>
    <waypoint name="STDBY" x="49.5" y="100.1"/>
    <waypoint name="1" x="-14.8" y="157.0"/>
    <waypoint name="2" x="175.7" y="85.2"/>
    <waypoint name="3" x="140.0" y="-2.3"/>
    <waypoint name="4" x="-49.6" y="66.3"/>
    <waypoint alt="215.0" name="AF" x="160.6" y="-4.7"/>
    <waypoint alt="185.0" name="TD" x="28.8" y="57.0"/>
    <waypoint name="_BASELEG" x="152.4" y="-45.9"/>
    <waypoint name="CLIMB" x="-139.7" y="88.2"/>
  </waypoints>
  <sectors>
    <sector color="orange" name="Survey">
      <corner name="1"/>
      <corner name="2"/>
      <corner name="3"/>
      <corner name="4"/>
    </sector>
  </sectors>
  <modules>
    <module name="nav" type="catapult_takeoff"/>
    <module name="nav" type="survey_polygon"/>
  </modules>
  <exceptions/>
  <blocks>
    <block name="Wait GPS">
      <set value="1" var="autopilot.kill_throttle"/>
      <while cond="!GpsFixValid()"/>
    </block>
    <block name="Geo init">
      <while cond="LessThan(NavBlockTime(), 10)"/>
      <call_once fun="NavSetGroundReferenceHere()"/>
    </block>
    <block name="Holding point">
      <set value="1" var="autopilot.kill_throttle"/>
      <attitude roll="0" throttle="0" vmode="throttle"/>
    </block>
    <block group="home" key="t" name="Takeoff" strip_button="Takeoff (wp CLIMB)" strip_icon="takeoff.png">
      <exception cond="GetPosAlt() > GetAltRef()+25" deroute="Standby"/>
      <set value="0" var="autopilot.kill_throttle"/>
      <set value="0" var="autopilot.flight_time"/>
      <go from="HOME" pitch="15" throttle="1.0" vmode="throttle" wp="CLIMB"/>
    </block>
    <block group="home" key="Ctrl+a" name="Standby" strip_button="Standby" strip_icon="home.png">
      <circle radius="nav_radius" wp="STDBY"/>
    </block>
    <block name="Catapult Take-Off" strip_button="Takeoff CATAPULT" strip_icon="bungee_launch.png">
      <exception cond="GetPosAlt() > GetAltRef()+25" deroute="Prepare survey"/>
      <set value="0" var="autopilot.kill_throttle"/>
      <set value="0" var="autopilot.flight_time"/>
      <call fun="nav_catapult_run(WP_CLIMB)"/>
      <deroute block="Prepare survey"/>
    </block>
    <block name="Prepare survey">
      <set value="DEFAULT_CIRCLE_RADIUS" var="nav_radius"/>
      <call_once fun="nav_compute_baseleg(WP_4, WP_1, WP__BASELEG, nav_radius)"/>
      <circle radius="nav_radius" until="NavCircleCount() > 0.5" wp="_BASELEG"/>
      <circle radius="nav_radius" until="And(NavQdrCloseTo(DegOfRad(baseleg_out_qdr)-(nav_radius/fabs(nav_radius))*10), 10 > fabs(GetPosAlt() - WaypointAlt(WP__BASELEG)))" wp="_BASELEG"/>
    </block>
    <block name="Route 4-1">
      <go approaching_time="0" from="4" hmode="route" wp="1"/>
    </block>
    <block group="nav_pattern" name="Poly Survey">
      <call_once fun="nav_survey_polygon_setup(WP_1,4,110,30,30,40,GetAltRef()+50)"/>
      <call fun="nav_survey_polygon_run()"/>
      <deroute block="Route 3-2"/>
    </block>
    <block name="Route 3-2">
      <go approaching_time="0" from="3" hmode="route" wp="2"/>
    </block>
    <block group="land" name="Land Right AF-TD" strip_button="Land right (wp AF-TD)" strip_icon="land-right.png">
      <set value="DEFAULT_CIRCLE_RADIUS" var="nav_radius"/>
      <deroute block="land"/>
    </block>
    <block group="land" name="Land Left AF-TD" strip_button="Land left (wp AF-TD)" strip_icon="land-left.png">
      <set value="-DEFAULT_CIRCLE_RADIUS" var="nav_radius"/>
      <deroute block="land"/>
    </block>
    <block name="land">
      <call_once fun="nav_compute_baseleg(WP_AF, WP_TD, WP__BASELEG, nav_radius)"/>
      <circle radius="nav_radius" until="NavCircleCount() > 0.5" wp="_BASELEG"/>
      <circle radius="nav_radius" until="And(NavQdrCloseTo(DegOfRad(baseleg_out_qdr)-(nav_radius/fabs(nav_radius))*10), 10 > fabs(GetPosAlt() - WaypointAlt(WP__BASELEG)))" wp="_BASELEG"/>
    </block>
    <block name="final">
      <exception cond="GetAltRef() + 10 > GetPosAlt()" deroute="flare"/>
      <go from="AF" hmode="route" vmode="glide" wp="TD"/>
    </block>
    <block name="flare">
      <go approaching_time="0" from="AF" hmode="route" throttle="0.0" vmode="throttle" wp="TD"/>
      <attitude roll="0.0" throttle="0.0" until="FALSE" vmode="throttle"/>
    </block>
  </blocks>
</flight_plan>
